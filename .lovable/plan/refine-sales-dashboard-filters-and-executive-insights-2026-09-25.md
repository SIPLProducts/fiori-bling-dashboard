# Refine Sales Dashboard filters and Executive Insights

## Changes

- Remove the **Generate Detailed Variance Report** button from **Executive Insights**.
- Keep the main **PDF** download in the Sales Dashboard header unchanged.
- Remove the now-unused download and loading properties passed into the Executive Insights/quarter analysis area.
- Give the **Smart filters** panel a restrained enterprise accent using the dashboard’s existing semantic blue/teal colors:
  - a lightly tinted header and clearer active-filter count;
  - subtle emphasis around the expanded filter area;
  - selected filter tags with an accent border/background and a clear remove state.
- Preserve the existing filter fields, spacing, layout, search, reset, expand/collapse, selected-tag clearing, and calculations.

## Verification

- Confirm the variance-report button is gone while the header PDF action remains.
- Confirm selected filter tags are readable and can still be removed.
- Check expanded and collapsed filter states at desktop and mobile widths.
- Confirm the latest preview build has no errors.
