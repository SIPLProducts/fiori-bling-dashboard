# Hide the duplicate Sales Dashboard title

## Change
- Remove the visible **Management Sales Dashboard** heading from the Sales Dashboard content area.
- Keep the portal top bar, dashboard controls, filters, cards, charts, data, permissions, and routes unchanged.
- Preserve the supporting date-range text only if it remains useful without the duplicate heading; otherwise align the controls cleanly in the freed space.

## Held for later
- Do not add the BSARK chart yet.
- Do not change SAP save/update keys yet.
- Do not change the current XBLNR → Reference or BSCHL → Posting Key mappings.

## Verification
- Confirm the duplicate heading is absent on desktop and mobile.
- Confirm all Sales Dashboard controls and charts still work without layout overlap.
