# Move Main Group charts below Total Sales

## Update
- Move **Sales by Main Group (Amount)** from its current three-column analytics row to directly below the **Total Sales (Amount)** tile.
- Move **Main Group vs Sub Group (Amount)** from the bottom of the dashboard to the same new row.
- Display both charts side by side on desktop and stack them vertically on smaller screens.
- Keep the existing chart styles, expand controls, amount calculations, and click-to-view subgroup behavior unchanged.
- Remove their previous placements so each chart appears only once.

## Verification
- Confirm the order is Total Sales, then the two requested Main Group charts.
- Confirm both charts respond to the existing filters and share the selected Main Group drill-down state.
- Check desktop and mobile layouts for readable labels and no overlap.
