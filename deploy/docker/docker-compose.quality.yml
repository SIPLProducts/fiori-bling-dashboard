# MIS Portal — QUALITY stack (isolated network + volumes)
# Run from /opt/MIS_Projects/Quality/backend:
#   docker compose --env-file .env -f docker-compose.quality.yml up -d --build
name: mis-quality

networks:
  mis_quality:
    name: mis_quality_net

volumes:
  q_db_data:
  q_storage_data:

services:
  db:
    image: supabase/postgres:15.8.1.049
    container_name: mis_q_db
    restart: unless-stopped
    networks: [mis_quality]
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
      JWT_SECRET: ${JWT_SECRET}
    volumes:
      - q_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 12

  migrate:
    image: supabase/postgres:15.8.1.049
    container_name: mis_q_migrate
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ../../supabase/migrations:/migrations:ro
    entrypoint:
      - bash
      - -c
      - |
        set -e
        for f in $$(ls /migrations/*.sql | sort); do
          echo "==> applying $$f"
          psql -h db -U postgres -d postgres -v ON_ERROR_STOP=1 -f "$$f"
        done
        echo "migrations complete"
    restart: "no"

  auth:
    image: supabase/gotrue:v2.170.0
    container_name: mis_q_auth
    restart: unless-stopped
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      API_EXTERNAL_URL: ${API_EXTERNAL_URL}
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://supabase_auth_admin:${POSTGRES_PASSWORD}@db:5432/postgres
      GOTRUE_SITE_URL: ${SITE_URL}
      GOTRUE_URI_ALLOW_LIST: ${SITE_URL}
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_EXP: 3600
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated
      GOTRUE_DISABLE_SIGNUP: "false"
      GOTRUE_EXTERNAL_EMAIL_ENABLED: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "true"

  rest:
    image: postgrest/postgrest:v12.2.3
    container_name: mis_q_rest
    restart: unless-stopped
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      PGRST_DB_URI: postgres://authenticator:${POSTGRES_PASSWORD}@db:5432/postgres
      PGRST_DB_SCHEMAS: public,storage
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"

  realtime:
    image: supabase/realtime:v2.34.7
    container_name: mis_q_realtime
    restart: unless-stopped
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      PORT: 4000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: supabase_admin
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_NAME: postgres
      DB_ENC_KEY: supabaserealtime
      API_JWT_SECRET: ${JWT_SECRET}
      SECRET_KEY_BASE: ${REALTIME_SECRET_KEY_BASE}
      ERL_AFLAGS: -proto_dist inet_tcp
      RLIMIT_NOFILE: "10000"
      APP_NAME: realtime
      SEED_SELF_HOST: "true"

  storage:
    image: supabase/storage-api:v1.19.3
    container_name: mis_q_storage
    restart: unless-stopped
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      ANON_KEY: ${ANON_KEY}
      SERVICE_KEY: ${SERVICE_ROLE_KEY}
      PGRST_JWT_SECRET: ${JWT_SECRET}
      DATABASE_URL: postgres://supabase_storage_admin:${POSTGRES_PASSWORD}@db:5432/postgres
      FILE_SIZE_LIMIT: "52428800"
      STORAGE_BACKEND: file
      FILE_STORAGE_BACKEND_PATH: /var/lib/storage
      TENANT_ID: quality
      REGION: local
      GLOBAL_S3_BUCKET: stub
      GLOBAL_S3_PROTOCOL: http
    volumes:
      - q_storage_data:/var/lib/storage

  meta:
    image: supabase/postgres-meta:v0.86.0
    container_name: mis_q_meta
    restart: unless-stopped
    networks: [mis_quality]
    depends_on:
      db: { condition: service_healthy }
    environment:
      PG_META_PORT: 8080
      PG_META_DB_HOST: db
      PG_META_DB_PORT: 5432
      PG_META_DB_NAME: postgres
      PG_META_DB_USER: supabase_admin
      PG_META_DB_PASSWORD: ${POSTGRES_PASSWORD}

  kong:
    image: kong:2.8.1
    container_name: mis_q_kong
    restart: unless-stopped
    networks: [mis_quality]
    depends_on: [auth, rest, realtime, storage, meta]
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /home/kong/kong.yml
      KONG_DNS_ORDER: LAST,A,CNAME
      KONG_PLUGINS: request-transformer,cors,key-auth,acl
      SUPABASE_ANON_KEY: ${ANON_KEY}
      SUPABASE_SERVICE_KEY: ${SERVICE_ROLE_KEY}
      DASHBOARD_USERNAME: ${DASHBOARD_USERNAME}
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD}
    volumes:
      - ./supabase/kong.yml:/home/kong/kong.yml:ro

  studio:
    image: supabase/studio:20250224-d10db0f
    container_name: mis_q_studio
    restart: unless-stopped
    networks: [mis_quality]
    depends_on: [meta, kong]
    ports:
      - "127.0.0.1:8082:3000"
    environment:
      STUDIO_PG_META_URL: http://meta:8080
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      DEFAULT_ORGANIZATION_NAME: SIPL
      DEFAULT_PROJECT_NAME: MIS Quality
      SUPABASE_URL: http://kong:8000
      SUPABASE_PUBLIC_URL: ${API_EXTERNAL_URL}
      SUPABASE_ANON_KEY: ${ANON_KEY}
      SUPABASE_SERVICE_KEY: ${SERVICE_ROLE_KEY}

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: mis-portal:quality
    container_name: mis_q_app
    restart: unless-stopped
    networks: [mis_quality]
    depends_on: [kong]
    ports:
      - "127.0.0.1:8081:3000"
    volumes:
      - ../frontend/dist:/app/dist:ro
    environment:
      NODE_ENV: production
      PORT: 3000
      SUPABASE_URL: ${VITE_SUPABASE_URL}
      SUPABASE_PUBLISHABLE_KEY: ${VITE_SUPABASE_PUBLISHABLE_KEY}
      SUPABASE_PROJECT_ID: ${VITE_SUPABASE_PROJECT_ID}
      SUPABASE_SERVICE_ROLE_KEY: ${SERVICE_ROLE_KEY}
      VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
      VITE_SUPABASE_PUBLISHABLE_KEY: ${VITE_SUPABASE_PUBLISHABLE_KEY}
      VITE_SUPABASE_PROJECT_ID: ${VITE_SUPABASE_PROJECT_ID}
